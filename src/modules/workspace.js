"use strict";

class Workspace {
	constructor(title, description, todos, color) {
		this.title = title;
		this.description = description;
		this.todos = todos;
		this.color = color;
	}
}

export default Workspace;